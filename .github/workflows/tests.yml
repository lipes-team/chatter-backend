name: Tests
on:
  pull_request:
    branches:
      - main
      - development
env:
  PORT: 5678
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install modules
        run: pnpm install
      - name: Run tests
        run: pnpm test
