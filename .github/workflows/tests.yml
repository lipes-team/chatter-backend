name: Tests
on:
  pull_request:
    branches:
      - main
      - development
environment: actions_test
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install modules
        run: npm install
      - name: Run tests
        run: npm run test
